<% provide(:title, 'Sign up') %>
<h1>Sign Up</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@user) do |f| %>
      <%= render 'partials/error_messages' %>
      <%= render 'form' %>
      <% end  %>
<%= link_to 'Back', users_path %>
